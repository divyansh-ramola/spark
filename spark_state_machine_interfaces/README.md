Spark State Machine Interfaces

This package defines ROS 2 interfaces used by the spark_state_machine package.

Interfaces:
- msg/GripperStatus.msg
- srv/MoveGripper.srv

Build with colcon so Python modules are generated under rosidl:
